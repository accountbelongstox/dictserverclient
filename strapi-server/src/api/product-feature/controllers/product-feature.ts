

/**
 * product-feature controller
 */

import { factories } from '@strapi/strapi';
const { createCoreController } = factories;
const e = createCoreController('api::product-feature.product-feature');

export default e;
